# SELA-handoff.md — Patient follow V7.1.0

> **產生時機：** V7.1.0「雙軌 logo 系統落地」里程碑完成。
> 這是這個專案第二次正式對齊 Kit（第一次是內部 V7.0.0 但未交付，併入 V7.1.0），也是 **Kit V1.13.0 雙軌 logo 系統第一次在真實業務 app 上完整落地** 的案例。

---

## 〇、專案速覽

- **專案名稱：** Patient follow（彰濱癌症中心個管病患追蹤系統）
- **專案類型：** React PWA（純前端，IndexedDB 儲存，無後端）
- **技術棧：** React 18 + TypeScript + Vite + TailwindCSS + Zustand + Dexie.js v4 + pptxgenjs / docx
- **規模：** 約 80 個檔案、~30k 行 TS/TSX；13 張 IndexedDB 表
- **使用 Kit 版本：** V1.16.0（跨版升級：V1.7.0 → V1.16.0 一次到位）
- **完成版本：** V7.1.0
- **完成日期：** 2026-06-14
- **里程碑類型：** 雙軌 logo 落地 + 跨版 Kit 對齊

---

## 一、用 Kit 的整體感受

### 預期外的順利 — 配色對齊鐵律回流案例

**Kit V1.8.0 把本專案當成配色對齊鐵律的源案例 — 直接引用 `#5B8FB9` vs `#5A7A8B` 的決策。**

打開 Kit V1.16.0 的 README 看 V1.8.0 版本歷程，發現：

> **conventions/colors.md** §3 新增「配色對齊鐵律」：
> - 既有專案對齊 Kit 時，**不主動改配色**（即使視覺上幾乎不可分）
> - Patient Follow 案例 `#5B8FB9` vs Kit 預設 `#5A7A8B` 為證據

**這個機制有效**。當 V7.0.0 SELA 跟我講「配色不是絕對，除了首次生成外，後面對齊換色要問我」時，那只是這個專案的內部規矩；現在它成了 Kit 全體規範。下一個既有專案要對齊 Kit 時，不會再有 Claude 自作主張改主色。

### 預期外的順利 — 雙軌 logo §10.2 補救四步真的派上用場

Gemini 生成的第一張 logo 是「假棋盤格」當透明背景（不是真的 RGBA alpha）。本來想直接用 §10.2 floodfill 四步補救，但**棋盤格的白↔灰交錯讓單純 floodfill 沒辦法跨越**（thresh 80 吃不進、thresh 太大會吃藍色框）。

**第二張 Gemini 重生為「純白頁面底」就乾淨了** — SELA 直覺判斷「白底比假灰格容易」是對的。教訓：**§10.2 適用單一純色背景，AI 給的假棋盤格反而最難處理**。

### 預期外的順利 — claude-init.md 步驟 9 強制 handoff 確實生效

V1.7.1 的「強制 handoff 評估」機制有效。連 V7.0.0（沒交付的內部版）我都產出了 handoff（雖然只是內部留檔），V7.1.0 這份是承接 V7.0.0 內容再加雙軌 logo 反饋。**沒有「忘記寫 handoff」的情況**。

### 預期外的卡住

- **`templates/CLAUDE-template.md` 仍然不適用既有 30k 行專案：** Kit V1.16.0 README 寫得清楚「§2 改現有專案」，但 `CLAUDE.md` §2 本身對「既有大型專案對齊 Kit」的指引仍偏簡略。我這次靠 V7.0.0 sandbox 重設後的記憶 + 上次 handoff 提的「鐵律/建議/順便/不做」四級清單法自己走完，但這套方法論還沒進 Kit。
- **Kit V1.13.0 雙軌 logo §10.2 預設 AI 會給「白底」，但實際上 Gemini 第一次給的是「假棋盤格」。** §10.2 四步補救能應付白底，但假棋盤格要走更複雜的 floodfill + dilate + 校色三段式。
- **sandbox 重設讓 V7.0.0 的對齊工作全部消失。** 雖然對話脈絡保留，但檔案系統重設要重做一輪。這不是 Kit 的問題，是 Claude 平台機制，但**對「現有專案對齊 Kit」這種跨多輪的長任務影響很大**。我寫進坑庫 #17。

### 對 Kit 的整體評價

- ✓ **Kit V1.7→V1.16 的演進路線正確** — 從「規範」升級到「規範 + 回流機制 + 機制執行率優化 + 雙軌品牌系統」，每步都對。
- ✓ **被引用為配色案例感覺很好** — 慘痛經驗變成大家受益的規範，這是 Kit 機制的勝利。
- ✓ **雙軌 logo 系統落地後個管師的反饋（預測）：** 終於能在分頁/dock 認出系統，不再跟其他 SELA 工具混在一起。
- ✗ **「現有專案對齊 Kit」這條路徑的 SOP 還沒寫足。** §2.6（V1.8.0 加的）方向對了，但對「跨多版本一次對齊」（如本案 V1.7→V1.16）沒明說怎麼操作。

---

## 二、發現的「跨專案通用坑」（建議進 Kit）

### 提案前的兩個檢查（V1.16.0 規範）

#### 檢查 1：先 grep Kit

```bash
# 確認沒重複
grep -in "sandbox.*重設\|工作環境消失\|對話中途.*消失" SELA-Starter-Kit/conventions/cross-project-pitfalls.md
# → 結果為空、可提案

grep -in "AI.*假棋盤格\|fake checkerboard\|偽透明" SELA-Starter-Kit/logo/CLAUDE.md
# → 結果為空、可提案
```

### 強烈建議加坑

#### 坑 #59「sandbox 中途重設導致長任務工作消失」

- **症狀**：對話進行中、Claude 工作目錄（如 `/home/claude/case-manager-web`）整個消失，但對話上下文還在。原本以為 V7.0.0 已完成，繼續做 V7.1.0 時發現檔案全沒了。
- **原因**：Claude 平台 sandbox 有定期重設機制，與專案內容無關。
- **做法**：
  1. **每個重要里程碑立刻產 zip 給用戶下載**（不只是「完成時才打包」，是「中途也要」）
  2. CLAUDE.md 寫進專案後，用戶手上必須有最新一份完整 source zip
  3. sandbox 重設後可用「對話脈絡 + 用戶上次 source zip」重建 — 不會 100% 還原但能補上對話脈絡的決策
  4. 不要假設工作環境永遠在
- **影響範圍**：所有跨多輪、需要長時間累積成果的 Claude 對話 — 特別是「對齊 Kit」「重構大型專案」「逐步迭代設計」這類情境
- **檢查 1 結果**：grep Kit「sandbox / 工作環境消失」→ 全無命中，**新坑**

#### 坑 #60「AI 生 logo 給『假棋盤格』當透明背景」

- **症狀**：Gemini 等 AI 工具生成 logo 時，**把棋盤格圖案畫進圖裡當作偽透明背景**，圖檔模式是 RGB（沒 alpha channel）。直接縮成 favicon 會帶著棋盤格紋路。
- **原因**：AI 工具誤解「透明背景」prompt — 把「設計師工具裡顯示透明的棋盤格」當成圖案畫進去。Gemini 比 DALL-E/Midjourney 更容易這樣做。
- **做法**：
  1. **先檢查圖檔模式：** `Image.open(file).mode == 'RGB'` 而不是 `'RGBA'` 就是 fake transparency。
  2. **要求重生 + prompt 改成「pure white page background (will be removed)」**，反而比要求「transparent」更可靠 — AI 給白底容易處理。
  3. **如果 AI 堅持給假棋盤格**：走兩階段處理 — (a) floodfill 從四角 thresh=100 吃掉棋盤格 (b) ndimage.binary_dilation 2-3 像素吃掉邊緣 anti-aliasing 光暈 (c) 對藍色框架像素統一校色到目標色。
- **影響範圍**：所有走 logo §10.2 工作流的專案，特別是用 Gemini 的
- **檢查 1 結果**：grep Kit「假棋盤格 / fake checkerboard / 偽透明」→ 全無命中，**新坑**
- **建議寫進**：`logo/CLAUDE.md` §10.2 開頭加「**先檢查圖檔模式（RGB ≠ 透明）**」+ §10.2.5 加「假棋盤格三段式處理」子節

### 可加但等更多證據確認

#### 觀察「`使用說明書.md` + `cp` 同步機制」是不是通用模式

- **本案發生**：V6.9.3 時三份說明書互不同步（USER_GUIDE / 使用說明書 / 功能使用說明書），V7.0.0 立規矩「中文檔名為主版 + cp 出英文檔名複本」解決。
- **是否進 Kit**：上次 V1.8.0 已經明寫「USER_GUIDE.md 同步規則暫緩、N=1」。**本案是 N=1 + 1（仍同專案）**，不算新證據。建議**仍暫緩，等其他有非開發者受眾的專案再評估**。

---

## 三、發現的「跨專案設計模式」

### 1. 「跨多版本一次對齊」的策略

- **本案發生情境**：Kit 從 V1.7.0 跳到 V1.16.0（中間 8 版沒看過），不是逐版升級而是一次到位。我沒有一條條讀 V1.8 / V1.9 / V1.10 / ... / V1.16 的差異，而是：
  1. 先讀 V1.16.0 的 README 版本歷程當「導覽」
  2. 從 V1.8.0 的「配色對齊鐵律」確認 Kit 已收下我們的反饋（驗證機制有效）
  3. 從 V1.13.0「雙軌 logo」抓本案要做的核心新功能
  4. 從 V1.16.0 抓最新坑（#57 解壓不限 maxdepth）
  5. 中間版本（V1.9~V1.12 / V1.14~V1.15）的小改動不主動套用，等踩到再說
- **可推廣的原則**：**跨多版本對齊時，不要逐版套用** — 用最新版 README 當地圖、識別「新功能」vs「微調」、新功能補齊、微調等踩到。
- **代價 / 取捨**：可能漏掉一些「微調但實用」的規範。但避免被中間版的小改動淹沒、保持注意力在重點。
- **建議寫入**：`CLAUDE.md` §2 加 §2.7「跨多版本對齊策略」，引用本案為範例。

### 2. 「sandbox 重設 + 對話脈絡保留」的還原工作流

- **本案發生情境**：V7.0.0 做完後 sandbox 重設，但對話脈絡完整保留（Claude 還記得做過什麼）。我從 V6.9.3 source zip 重建 + 對話脈絡指引重做一輪。
- **可推廣的原則**：**對話脈絡是最有韌性的單一真相** — 用戶手上的最新 source zip 是物理檔案備份。兩者結合 = 完整還原能力。
- **代價 / 取捨**：要重跑一輪所有指令（半小時級的時間）。但比起「全部重來」好太多。
- **建議寫入**：跟坑 #59 配套，寫進 Kit 的「Claude 平台行為」章（如果有的話）或 `templates/claude-init.md` 加一節「sandbox 重設下的還原流程」。

---

## 四、Kit 該瘦身或調整的地方

### Kit 規範修改建議

#### 1. `logo/CLAUDE.md` §10.2 補「先檢查圖檔模式 + 假棋盤格處理」

- **現狀**：§10.2 第 3 步「Claude 評估能否直接用」提到「背景（透明 PNG 最理想；白底 / 純色底次之；複雜背景需去背）」，但**沒提「假棋盤格」這個 AI 常見坑**。
- **建議改成**：第 3 步開頭加「**先用 `Image.open(file).mode` 確認真的是 RGBA、不是 RGB+假棋盤格**」+ §10.2.5 加「假棋盤格三段式」子節（floodfill 100 + dilate 2-3 + 校色到目標色）。
- **理由**：Gemini 常給假棋盤格，現有 §10.2 四步補救主要針對「白底圓角縫隙」設計，假棋盤格要走更複雜的路。
- **本案證據**：Patient follow V7.1.0 logo 處理過程。

#### 2. `CLAUDE.md` §2 補「§2.7 跨多版本對齊策略」

- **現狀**：§2「改現有專案」+ §2.6（V1.8.0 加的）涵蓋「對齊 Kit」，但沒指引「跨多版本一次對齊」。
- **建議改成**：§2.7「跨多版本對齊」— 用最新 README 當地圖、抓新功能、忽略微調、踩到再補。引用本案 V1.7→V1.16 為範例。
- **理由**：現實上 SELA 不會每次 Kit 升版都立刻把所有專案對齊，會累積 5-10 版才一起做。

#### 3. `conventions/cross-project-pitfalls.md` 加「Claude 平台行為」章

- **現狀**：32 條坑按 A-L 章分類，沒有「平台行為」章。
- **建議改成**：新增 M 章「Claude / AI 平台行為」，第一條就是坑 #59 sandbox 重設。未來其他平台行為（如對話長度上限、tool call 失敗模式等）都收這章。
- **理由**：這類坑跨技術棧，跟業務無關但會影響開發流程。

### Kit 結構性建議

- **加入「對齊 Kit 過程」reference**：本案 V6.9.3 → V7.1.0 過程涵蓋了「修舊 bug + 加新功能 + 雙軌 logo + 跨多版本對齊 + sandbox 重設還原」幾乎所有能踩的坑，可以當第一個「對齊 Kit 過程示範」reference。但內容包含彰濱業務、要剝離。
- **「規範對齊」型 b+1 寫進 SPEC**（V1.8.0 從本案提的「N=1 暫緩」）：**現在 N=2**（本案 V7.0.0 + V7.1.0 都是規範對齊 b+1 升版）。是否升 N≥2 列為通用模式可考慮。

---

## 五、留在這個專案、**不要回流 Kit** 的東西

> 這節避免 Kit Claude 把彰濱業務邏輯誤收進 Kit。

- ❌ **CORE_14_CANCERS**（13~14 癌別主檔）— 純醫療業務
- ❌ **MDT_GROUP_CONFIG**（MDT 群組與排程）— 彰濱秀傳特定
- ❌ **CANCER_CODE_TO_MANAGER**（KAO / YANG / 林伯儒分工）— 此醫院特定
- ❌ **國健署 115 年 60 項指標定義** — 台灣醫療業務
- ❌ **PPTX/HTML 投影片版面細節** — 業務模板，跟 Kit 無關
- ❌ **Nordic-minimalist 設計系統 DESIGN_SYSTEM.md** — Kit colors.md 已涵蓋足夠
- ❌ **業務帳號 14002 / 11750** — 真實人員代碼
- ❌ **app logo 主視覺設計**（保護的手 + zen cairn + 心）— 本專案專屬，每個業務 app 各自獨特
- ⚠️ **「ensureDefaultAdmin per-account 檢查」原則** — 偏 Dexie + 帳號 seed 特定，**仍不要進 Kit**

---

## 六、Kit Claude 的建議行動清單

### 建議升 Kit 版本

**V1.17.0**（b+1，新增章節 + 兩條新坑）

理由：兩條新坑（#59 sandbox 重設、#60 假棋盤格）+ 一條結構性新增（M 章 Claude 平台行為）+ §2.7 跨多版本對齊策略 + logo §10.2.5 假棋盤格處理 + Kit V1.13.0 雙軌 logo 系統的第一個業務 app 落地案例驗證。屬於結構性新增不是 bug fix（不算 c+1），也沒大改既有規範（不算 a+1）。

### 必做

- [ ] **新增坑 #59「sandbox 中途重設導致長任務工作消失」**（新章 M「Claude 平台行為」）
- [ ] **新增坑 #60「AI 生 logo 給假棋盤格當透明背景」**（L 章 + logo/CLAUDE.md §10.2.5）
- [ ] **`logo/CLAUDE.md` §10.2 補「先檢查圖檔模式 + 假棋盤格三段式處理」**
- [ ] **`CLAUDE.md` §2 新增 §2.7「跨多版本對齊策略」**
- [ ] **更新 Kit README 版本紀錄**：V1.17.0 標明「Patient follow 第二次 handoff、雙軌 logo 第一個業務 app 落地、坑 #59 #60」

### 暫緩

- [ ] **USER_GUIDE 同步規則** — 仍 N=1（本案同專案兩次不算 N=2）
- [ ] **「規範對齊」型 b+1 寫進 SPEC** — N=2 但兩次都同專案，等第二個專案再考慮
- [ ] **「對齊 Kit 過程」reference 資料夾** — 結構性大改、等更多案例

### 不做

- [ ] **把 13 癌別 / MDT 群組 / 國健署指標收進 Kit** — 業務邏輯
- [ ] **改 Kit 預設醫療系統色為 #5B8FB9** — V1.8.0 已正式立鐵律「不為對齊而改」，Kit 標準仍是 #5A7A8B
- [ ] **把 Patient follow app logo 收進 Kit** — 每個業務 app 各自獨特，logo 不通用
- [ ] **把 zen cairn / 保護的手 主題收進 logo prompt 範本** — 業務專屬

---

## 七、給 Kit Claude 的最後備註

這份 handoff 有三個特殊性：

1. **跨版本一次到位**：從 Kit V1.7.0 直跳到 V1.16.0，中間 8 版沒參與。對於下次「久違升級」的 Claude 來說，本案的策略（最新版 README 當地圖、抓新功能、忽略微調）值得參考。

2. **配色對齊鐵律的閉環**：V1.8.0 從本專案反饋立鐵律，V7.1.0 又面臨同樣抉擇（要不要動 #5B8FB9 對齊 #5A7A8B），這次直接依鐵律處理、不需要再問。**機制設計 → 案例驗證 → 規範生效 → 後續案例自動遵守** 整條鏈路走通了。

3. **雙軌 logo 系統的首次業務 app 落地**：Kit V1.13.0 機制是設計好的（SELA dock 認不出系統的痛點觸發），但 Patient follow 是 SELA 第一次完整走完工作流 B（其他 AI 生圖 + Claude 處理）的業務 app。本案的處理流程（特別是假棋盤格那段）值得回流。

下次本專案達到實質里程碑（KAO/YANG 正式上線、加入 LY 第 14 癌別後的穩定版、或下次 Kit 大改）會再產出 handoff。

---

<div align="center">
  <br>
  <img src="src/assets/sela.svg" width="40" alt="SELA"/>
  <br>
  <p>Made by <strong>SELA</strong>, with <strong>Claude</strong> · V7.1.0</p>
</div>
